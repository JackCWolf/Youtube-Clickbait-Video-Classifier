from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Create a model that can classify clickbait videos based on the thumbnail',
    author='Jack Wolfgramm',
    license='MIT',
)
